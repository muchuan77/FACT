# FACT 项目开发规则（必须遵守）

本规则用于约束 FACT（网络舆情风险预警与谣言治理系统）的工程结构、模块职责边界与开发节奏。所有后续开发必须以“最小可运行版本（MVP）优先”为原则推进。

## 一、总体模块边界

- **fact_backend（Django + DRF）**：只负责业务后台与数据管理（用户、舆情、分析结果、风险预警、治理响应等）。  
  - 不允许在 Django 内直接加载/运行深度学习模型（包括 PyTorch/Transformers 权重加载）。
  - 允许通过 HTTP 调用 `fact_model_service` 获取推理结果。

- **fact_model_service（FastAPI + PyTorch + Transformers）**：只负责模型推理服务。  
  - 不处理复杂业务数据库逻辑（不直接承担业务数据的 CRUD 与权限）。
  - 对外提供清晰的推理接口（如：谣言识别、情感分析、关键词提取、风险辅助判断）。

- **fact_frontend（Vue3 + Vite + Element Plus + ECharts）**：只负责前端展示。  
  - 通过 Axios 调用 Django 后端 API，不直接调用模型服务（默认由 Django 统一转发/聚合）。

- **fact_crawler（Scrapy / Requests）**：只负责公开数据采集。  
  - 采集数据优先通过 Django API 入库（而非直写前端或绕过业务后台）。

- **datasets**：公开数据集与自采集数据存放区（数据文件可大、可不纳入 git，视后续策略而定）。
- **experiments**：训练/评估/基线实验代码（与在线推理解耦）。
- **docs**：论文材料、接口文档、系统设计文档。

## 二、MVP（第一阶段）范围约束

第一阶段只做“最小可运行链路”，不做复杂业务与扩展特性：

- **链路**：导入舆情数据 → Django 调用 FastAPI → 返回谣言识别结果 → 生成风险等级 → 前端展示
- **禁止**：编造不存在的功能（如管理员端、区块链、全网实时监测等），除非用户明确要求。
- **禁止**：在第一阶段安装/升级大量依赖、写训练代码、写复杂权限体系、写大而全模块。

## 三、接口与调用约定（建议）

- Django 作为业务聚合层，对外提供统一 REST API（前端只连 Django）。
- Django 调用 FastAPI 走内网/同机 HTTP（如 `http://localhost:8001`），并对超时/失败做降级处理（后续实现）。
- FastAPI 返回标准化 JSON：`{ ok, model, task, inputs, outputs, meta }`（后续实现）。

## 四、工程规范

- 所有子模块必须有各自 `README.md` 说明职责、启动方式（后续补齐）。
- 提交代码前保证目录结构清晰、命名统一、避免一次性引入“完整系统”级复杂度。
- 数据与模型权重默认不入库到 git（使用 `.gitignore` 过滤，必要时提供下载/生成脚本）。

