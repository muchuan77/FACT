# fact_backend（Django + DRF）

本模块用于 **业务后台**：用户、舆情数据、分析结果、风险预警、治理响应等业务与数据管理。

## 边界与约束

- 仅负责业务与数据编排（REST API）。
- **不得**在 Django 中直接加载/运行深度学习模型。
- 通过 HTTP 调用 `fact_model_service` 获取推理结果（后续实现）。

## 第一阶段（MVP）最小目标

- 提供舆情数据导入/录入的最小接口或管理方式（后续实现）。
- 提供“调用模型服务并保存结果”的最小接口（后续实现）。

